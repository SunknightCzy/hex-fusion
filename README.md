# hex-fusion

一个基于**六边形网格**的 2048 变体聚变游戏：滑动原子核，让它们按真实的核合成链发生聚变，一路从氢合成到超重核 ★。

单文件实现（`index.html`），无任何外部依赖，打开即玩。核合成链、热量系统、级联聚变动画、粒子特效、音效全部由原生 HTML/CSS/JS 完成。

## 玩法

- **六方向滑动**：将整行原子沿六轴推动（六边形轴向与键盘 QWEASD 一一对应，也支持触屏手势与方向按钮）。
- **聚变代替合并**：与普通 2048 的"相同数字合并"不同，这里只有当相邻原子满足真实核反应配方时才会聚变：
  - ¹H+¹H → ²H → ³He → ⁴He → ¹²C → ¹⁶O → ²⁰Ne → ²⁴Mg → ²⁸Si → ⁵⁶Fe（pp 链、三 α 过程、α 俘获、硅燃烧）
  - ⁵⁶Fe 之后进入人工轰击路径：铁种子 + 快中子束 → ¹⁹⁷Au → ²³⁸U → **★**（游戏终点，超重核）
- **热量系统（"别炸了"模式）**：
  - 每次聚变热度 +1，每回合自然散热 -1.5；
  - 热度达到上限即**堆芯失控**（游戏失败）。上限在合成 ★ 前为 16，合成 ★ 后提升至 32；
  - 合成出第一个 ★ 后解锁强化：棋盘上每个 ★ 每回合额外散热 -1，并在其周围随机生成一个 H/D 燃料。

## 特色

- **级联聚变动画**：一步滑动可触发多波连锁聚变，逐波播放；同一操作内后续波次自动加速，最后一波动画播完立即解锁输入，减少等待。
- **多波反应边框提示**：连锁反应进行时地图边框变为橙色脉冲闪烁。
- **显示 / 隐藏反应过程**：可切换为瞬时结算（保留粒子特效与产物生成动画，且不阻塞输入），追求快节奏操作。
- **自动玩**：一键开启随机方向自动移动（也可作为热量系统的压力测试）。
- **平均聚变统计**：最近 10 回合的滚动平均聚变数，实时反映近期节奏。
- **粒子特效与音效**：内聚粒子、爆裂冲击波、超新星庆典（合成 Au/★ 时全屏爆发），音效全部由 Web Audio 实时合成，无音频文件。

## 操作方式

| 操作 | 方式 |
| --- | --- |
| 移动 | 键盘 `Q W E A S D`（六方向）、鼠标/触屏拖拽、屏幕方向按钮 |
| 新游戏 | 顶栏「新游戏」按钮 |
| 开关反应过程 | 顶栏「反应过程：显示 / 隐藏」按钮 |
| 自动玩 | 顶栏「自动玩：开 / 关」按钮 |

## 运行

直接用浏览器打开 `index.html` 即可，无需构建、无需服务器。

## 目录结构

```
hex-fusion/
├── index.html          # 完整游戏（单文件，含样式、逻辑、特效、音效）
└── tools/
    └── heat_sim.js     # 热度平衡模拟脚本（复用游戏纯逻辑，可调参复跑）
```

## 技术说明

- 六边形棋盘采用轴向坐标（axial coordinates），半径 3（37 格）；滑动按巷道（lane）分段处理，支持不规则地图。
- 聚变判定：中心格 + 六邻格内凑齐配方即反应，参与原子必须彼此连通；同波反应用回溯求最大互不重叠集。
- 纯逻辑部分通过 `window.__hex2048Test` 暴露，供无 DOM 环境（如 Node）做测试与模拟。
- 背景标签页节流处理：标签页隐藏时暂停级联聚变、切回时清理积压粒子，避免动画堆积卡顿。

## 许可

[MIT](LICENSE)
